projection;
strict ( 2 );
use draft;

define behavior for Zsk_c_INVOICE //alias <alias_name>
{
  use create;
  use update;
  use delete;
  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;
}